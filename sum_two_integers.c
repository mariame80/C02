#include <stdio.h>

int main() {
    int number1, number2, sum;

    scanf("%d %d", &number1, &number2);

    // Calculate the sum
    sum = number1 + number2;

    // Display the sum
    printf("La somme est : %d\n", sum);

    return 0;
}
